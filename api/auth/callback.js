import {setSession} from "../_lib/auth.js";import {user} from "../_lib/github.js";function parseCookie(h){return Object.fromEntries((h||"").split(";").map(x=>x.trim().split("=")).filter(x=>x[0]).map(x=>[x[0],x.slice(1).join("=")]))}export default async function handler(req,res){try{const q=new URL("https://local"+(req.url||"")).searchParams;const cookies=parseCookie(req.headers.cookie);if(!q.get("state")||q.get("state")!==cookies.cms_oauth_state)throw Error("درخواست ورود معتبر نیست.");const code=q.get("code");if(!code)throw Error("کد ورود دریافت نشد.");const base=(process.env.APP_URL||("https://"+req.headers.host)).replace(/\/$/,"");const rr=await fetch("https://github.com/login/oauth/access_token",{method:"POST",headers:{"Accept":"application/json","Content-Type":"application/json"},body:JSON.stringify({client_id:process.env.GITHUB_CLIENT_ID,client_secret:process.env.GITHUB_CLIENT_SECRET,code,redirect_uri:base+"/api/auth/callback"})});const tok=await rr.json();if(!tok.access_token)throw Error(tok.error_description||"توکن GitHub دریافت نشد.");const u=await user(tok.access_token);const allowed=(process.env.ALLOWED_GITHUB_LOGIN||"esalati").toLowerCase();if(String(u.login).toLowerCase()!==allowed)return res.status(403).send("این حساب اجازه مدیریت سایت را ندارد.");setSession(res,{token:tok.access_token,login:u.login,name:u.name||u.login});res.redirect("/admin/")}catch(e){res.status(400).send("خطا در ورود: "+String(e.message||e))}}